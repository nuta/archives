{
  "name": "hyperutils",
  "version": "0.0.1",
  "description": "An utility library to prevent `dependency hell'.",
  "private": true,
  "main": "lib/index.js",
  "gypfile": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "nan": "^2.7.0"
  }
}
