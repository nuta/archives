#!/bin/sh
set -ue
ARCH=$1

if [ "$ARCH" = "host" ]; then
  node-gyp rebuild
  ARCH=$(node -e "console.log(process.arch)")
else
  node-gyp rebuild --arch $ARCH
fi

mkdir -p native/$ARCH
mv build/Release/*.node native/$ARCH
