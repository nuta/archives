{
  "name": "nodejs-runtime",
  "version": "0.0.1",
  "description": "MakeStack Node.js app runtime environment.",
  "gypfile": true,
  "private": true,
  "main": "lib/index.js",
  "scripts": {
    "build": "node-gyp rebuild --arch $ARCH && mkdir -p native/$ARCH && mv build/Release/*node native/$ARCH && rm -rf build"
  },
  "dependencies": {
    "nan": "^2.7.0"
  }
}
