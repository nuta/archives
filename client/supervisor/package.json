{
  "name": "makestack-supervisor",
  "version": "0.0.1",
  "description": "MakeStack Linux Supervisor.",
  "private": true,
  "main": "dist/index.js",
  "scripts": {
    "prepublish": "npm run build",
    "build": "tsc --pretty"
  },
  "dependencies": {
  },
  "devDependencies": {
    "@types/node": "^8.0.53",
    "typescript": "^2.6.1"
  }
}
