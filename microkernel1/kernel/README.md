# kernel
The `kernel` is a set of essential components for almost all Resea applications:

- system call handler
- channel server
- thread server
- memory server
- datetime server
- zero-filled pager
