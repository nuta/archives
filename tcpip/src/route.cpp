#include "tcpip.h"
#include <resea.h>
#include "ipv4.h"
#include "route.h"
#include "device.h"


// Looks up the appropriate net device in the routing table. It Returns
// a channel connected to the net device.

/* XXX: implemented in device.cpp for now
struct net_device *tcpip_route(struct addr *addr) {

    return ;
}
*/
