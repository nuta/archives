{
  "name": "@makestack/supervisor",
  "version": "0.1.0",
  "description": "MakeStack Linux Supervisor.",
  "private": true,
  "main": "dist/index.js",
  "scripts": {
    "cov": "istanbul cover _mocha test/*.spec.ts",
    "test": "mocha test/*.spec.ts",
    "test:live": "mocha -w test/*.spec.ts",
    "pretest": "tsc",
    "prepublish": "npm run build",
    "build": "tsc",
    "build:live": "tsc -w"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/chai": "^4.0.6",
    "@types/jszip": "^3.1.2",
    "@types/mocha": "^2.2.44",
    "@types/nock": "^8.2.1",
    "@types/node": "^8.0.53",
    "@types/sinon": "^4.0.0",
    "chai": "^4.1.2",
    "istanbul": "^1.1.0-alpha.1",
    "jszip": "^3.1.5",
    "mocha": "^4.0.1",
    "mock-fs": "^4.4.2",
    "nock": "^9.1.3",
    "sinon": "^4.1.2",
    "source-map-support": "^0.5.0",
    "ts-node": "^3.3.0",
    "typescript": "^2.6.1"
  }
}
