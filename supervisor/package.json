{
  "name": "@makestack/supervisor",
  "version": "0.0.1",
  "description": "MakeStack Linux Supervisor.",
  "private": true,
  "main": "dist/index.js",
  "scripts": {
    "cov": "istanbul cover _mocha",
    "test": "mocha",
    "test:live": "mocha -w",
    "pretest": "tsc --pretty",
    "prepublish": "npm run build",
    "build": "tsc --pretty",
    "build:live": "tsc --pretty -w"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/mocha": "^2.2.44",
    "@types/node": "^8.0.53",
    "istanbul": "^1.1.0-alpha.1",
    "mocha": "^4.0.1",
    "nock": "^9.1.3",
    "power-assert": "^1.4.4",
    "sinon": "^4.1.2",
    "source-map-support": "^0.5.0",
    "ts-node": "^3.3.0",
    "typescript": "^2.6.1"
  }
}
