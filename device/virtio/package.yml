name: virtio
category: application
id: 1004
license: Public Domain
summary: A device driver for virtio devices
description: # longer explanation

warning:     # some important things to note
info:        # what we should know

author: Seiya Nuta
email: nuta@seiya.me
homepage:    # e.g. http://example.com/foo/bar

# Required packages
requires: [io, cpp, libpci]
lang: c

# Interfaces which this package uses
uses: []

# Interfaces which this package implements
implements: [net_device, storage_device]


#
#  Type definitions
#
type:


#
#  Interface definitions
#
interface:

#
#  Config
#
config:
  SOURCES:
    set:
      - src/net_device.receive.c
      - src/net_device.transmit.c
      - src/net_device.get_hardware_address.c
      - src/storage_device.read.c
      - src/storage_device.write.c
      - src/handler.c
      - src/startup.c
      - src/virtio.c
      - src/virtio_net.c
      - src/virtio_blk.c
